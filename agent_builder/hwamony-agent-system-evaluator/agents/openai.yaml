interface:
  display_name: "Hwamony Agent Evaluator"
  short_description: "Turn live runs into rubric-backed evidence, stored evaluation artifacts, and the next improvement backlog"
  default_prompt: "Use $hwamony-agent-system-evaluator to test this system with run logs, rubric scores, and stored evidence, then turn the findings into the next implementation backlog."
