interface:
  display_name: "Hwamony Agent Implementer"
  short_description: "Turn a chosen system design into prompts, configs, runtime code, and history adapters"
  default_prompt: "Use $hwamony-agent-system-implementer to turn the selected architecture into an evaluation-ready baseline with prompts, configs, runtime code, and message-history adapters."
