interface:
  display_name: "Hwamony Prompt Architect"
  short_description: "Turn fragile prompts into schema-first, tool-aware prompt systems that are easier to ship"
  default_prompt: "Use $hwamony-prompt-architect to turn this fragile prompt into a reusable prompt system with model guidance, output schema, tool rules, and a recommendation on whether it should stay a prompt or become an agent workflow."
